machine:
  ghc:
    version: 7.10.1

dependencies:
  override:
    - cabal update
    - cabal sandbox init
    - cabal install QuickCheck
    - cabal install hspec
    - cabal configure --enable-tests
